# @griptix/types

This is the shared TypeScript and Pydantic schemas package for the Griptix platform.

## Features
- Shared TypeScript interfaces mapping directly to database entities.
- Codegen scripts compiling the FastAPI OpenAPI spec to Next.js TypeScript typings (`openapi-typescript`).
- Reusable type definitions for order states, biomechanical metrics, and user roles.
