# Route group: account pages (login, register, orders, profile, sizing wizard)
# Pages here share a layout with a minimal header (no full nav).
# Add layout.tsx here when the account layout is built.
